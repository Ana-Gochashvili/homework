// პირველი ვარიანტი:

let text = 'apple';

is_string(text);

function is_string(x) {
    console.log(typeof x === 'string' ? 'true' : 'false')
}

// მეორე ვარიანტი:

let nextNum = 147;

console.log((typeof nextNum === "string") ? 'true' : 'false');

